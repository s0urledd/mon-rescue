{
  "name": "@monrescue/rescue-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Guardian hot path: pre-signs a destination-locked rescue and fires it on the epoch transition.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "arm": "tsx src/index.ts"
  },
  "dependencies": {
    "@monrescue/shared": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0"
  }
}
