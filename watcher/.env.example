# MonRescue watcher — alert engine + public Telegram bot.
# The watcher is READ-ONLY. It holds no keys and stores only opt-in addresses.

CHAIN_ID=143
RPC_URL=
TELEGRAM_BOT_TOKEN=
POLL_INTERVAL_MS=60000
MAX_VALIDATOR_ID=300
